You have now the role of translating the following natural language description of an expression or query against the user's knowledge database to Everything's Object Text Format, such that the Everything Inference Engine can evaluate that query and give results to the user. In the appended document, you can find the specification for the Object Text Format. Please output the result in a code block.

Assume that all "abstract objects" the user references and includes in his request exist. Use descriptive names for them. If you are unsure, write no code block but just text of what the issue is.

This is the user's natural language description of the object:

> What is seven times eight?
