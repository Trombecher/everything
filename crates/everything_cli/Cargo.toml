[package]
name = "everything_cli"
description = "The Everything CLI lets you interface with .struct files."
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
colored = "3.1.1"
everything_structures = { path = "../everything_structures" }
