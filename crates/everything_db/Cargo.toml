[package]
name = "everything_db"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["fs", "rt", "io-util"] }
everything_objects = { path = "../everything_objects" }
everything_tff = { path = "../everything_tff" }
everything = { path = "../everything" }
memmap2 = "0.9.10"
