{
    (@NODE_MAP_SET, {
        (@CONTAINS, 1)
        (@CONTAINS, 2)
    })
    (@NODE_MAP_F, {
        (@COMPUTED, {
            (@NODE_ADD_LEFT, {(@NODE_PARAMETER, 0)})
            (@NODE_ADD_RIGHT, 2)
        })
    })
}